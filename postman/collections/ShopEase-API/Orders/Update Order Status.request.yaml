$kind: http-request
method: PUT
url: '{{baseUrl}}/api/orders/{{orderId}}/status'
order: 4000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    { "status": "shipped" }
